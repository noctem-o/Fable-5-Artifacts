# MYTHOS Report — Universality Stress → Theory Extraction
Executed live; raw outputs in `mythos.py`, figure `mythos_kill_test.png`. Companion docs: `theorems-and-certification.md`, `theorem3-proof.md`.

## Phase 1 — Falsification table (all attacks executed)

| # | Attack | Observable that breaks | Fake signature | Measured | Verdict |
|---|---|---|---|---|---|
| A1 | Estimator closure | degeneracy root of E_left−E_right; classical s̄ vs measured quantum crossing s*(N)/M | multiple roots; s̄ ≠ slope of s*(N) | unique root (1 sign change, both cases; global monotonicity neither needed nor claimed); s̄_class vs measured slope: Δ = +3.9e-5 (C1), −2.2e-4 (C4) — independent observable, never used in constructing S_corr | SURVIVES |
| A2 | Basis/representation | Γ∞ under prefactor chart N^α; Γ itself | Γ∞ shifts with chart | Γ is spectral (unitary-invariant by construction); Γ∞ spread across pref ∈ {1, N, N²}: 8e-5 (1/N model), 1e-5 (ln model); b shifts by +0.987, +0.986 per power of N — the exact theoretical chart covariance | SURVIVES |
| A3 | Finite-size pseudo-convergence | window-split Γ∞ | window-dependent drift > errors | low-N window: [0.14938, 0.15082]; high-N window tightens to [0.15020, 0.15028] onto S_corr = 0.15013 — convergence genuine, models merge with N | SURVIVES |
| A4 | Regime-splitting / branching | gap under forced opposite pipeline branch | branch-dependent gap at claim level | rel.diff 2.0e-3 in gap at the off-design branch (= 8e-5 in Γ units; attributable to the documented cubic-model degradation of the band never used there); binding cross-method check is float64 vs mp at ≤1.2e-6 | SURVIVES (caveat logged) |
| A5 | Hidden parameter leakage | blind prediction at never-tested point | prediction misses out-of-sample | S_corr signature = (ε,κ,g,ω) only, no N, no data. KILL TEST C4 = (ε=−0.5, κ=0.8 [first κ≠1], g=3.0): pred 0.15013 → measured bracket [0.14971, 0.15054], |mean−S| = 1e-5; pred 0.19505 → bracket [0.19473, 0.19581], |mean−S| = 2.2e-4. Both PASS criterion | SURVIVES |

## Phase 2 — Invariant extraction
**Invariant object:** 𝒮[H] := lim_{M→∞} −(1/M) log gap_min — spectral, invariant under U(ℋ) conjugation and drive reparametrization. **Candidate identity:** 𝒮[H] = instanton action of the principal 2×2 matrix symbol's lower sheet at the doubly degenerate energy: ∫ Im χ dx along the path defined chart-free by the characteristic-variety condition (h_A−E_c)(h_B−E_c) = |w(x,χ)|², with (s̄, E_c) fixed by double degeneracy. **Observable class for which Γ is stable:** 𝒪_spec = {gap_min, Im z_EP, (4χ_F^max)^{−1/2}, Kato/SW radius}, pairwise locked by Theorem 1 to relative O(e^{−cM}); excluded: w_direct (distinct class, exponent S_Agmon = 𝒮|_{ω→0}, PROVEN — Theorem 3). **Group:** U(ℋ_M) × Diff⁺(s) × U(1)² fiber gauge (invariant coupling = |w|²) × canonical transformations of (x,χ) preserving boundary structure. **Verdict on S_corr:** NOT fundamental — a Darboux-chart (number-phase) evaluation of 𝒮[h]. The falsified S_const was the gauge/chart-dishonest evaluation (diagonal coupling value instead of |w(x,χ)|²); its failure is *required* by the invariance, retrodictively.

## Phase 3 — RG structure
Flow in t = ln N: **dΓ/dt = −(Γ − Γ*(ω, landscape)), Γ* = 𝒮[h]** — line of fixed points parametrized by marginal (ω, ε, κ, g); single irrelevant direction, correction-to-scaling eigenvalue y = −1 (pure 1/N verified N = 64→280 across charts; lnN/N admixture bounded by model spread ±4e-4). Fixed-point structure REAL, not accidental (A3 + A5). Amplitude b: NOT universal and chart-dependent (shifts by α under pref→pref·N^α); chart-fixed values monotone in ω: b ≈ 0.24 (ω=0.05) → 0.36–0.38 (0.1–0.13) → 0.40–0.44 (0.2). Chart-invariant flow content: the eigenvalue −1 and the fixed line only.

## Phase 4 — Theorem candidate
**Assumptions.** (A1) H_M(s) = (H_A ⊕ (H_B+s)) + W, even sector, coefficients as specified; (A2) f has a unique nondegenerate interior minimum, f > E₀ elsewhere; (A3) ω ∈ (0, ω_c): lower symbol sheet retains two nondegenerate minima at degeneracy; (A4) coupling enters via |w(x,χ)|².
**Statement.** lim −(1/M) log gap_min = 𝒮[h]; same exponent for Im z_EP and (4χ_F^max)^{−1/2} (Theorem 1); 𝒮 in the number-phase chart = the linear-in-cosh closed quadrature (S_corr); 𝒮|_{ω→0} = S_Agmon, for which the matrix-element law is PROVEN (Theorem 3; error O(M^{2/3}) elementary, O(log M) conjectured).
**Status.** DERIVED + CERTIFIED incl. blind out-of-sample at κ≠1 (|Δ| ≤ 2.2e-4); spectral version NOT proven.
**Failure modes.** F1 multiple/degenerate h₋ minima — instanton min-rule untested; F2 ω ≥ ω_c — statement void; F3 other symmetry sectors untested; F4 1/N amplitude non-universal, log-corrections unresolved < ±4e-4; F5 proof gap: coupled-chain lower bound (positivity holds after ℤ₂ gauge ψ_A → −ψ_A; Riccati program plausibly extends; open).
**No-go:** not triggered.

## Round 2 addendum — constructor hardening, a builder bug, and the genuine gamma
**Hardening (user-proposed, referee-corrected).** The Delta(x) root-catcher proposal was adopted with three corrections: (i) lambda vanishes like sqrt at turning points — no integration singularity ever existed; (ii) patch-summing computes sequential actions and cannot "eliminate F1", which is well-*selection* physics; (iii) self-protection lemma: E_c is defined as the global dressed-sheet minimum, hence Delta(x) >= 0 on the real section and open allowed pockets are impossible at the gap-min energy (they arise only for excited crossings). The hardened `construct_invariant` enumerates all degeneracy roots, all dressed minima, all candidate actions, and flags competition. Regression: reproduces all six certified values to <= 5e-6.
**Tether-catch #4.** Forensics exposed a sign error in `quartic_from_roots` (V = f − 4kx(1−x) instead of +): the round-2 beta/gamma runs used f_design − 8kx(1−x). Both runs were *internally* consistent (prediction and measurement shared the wrong landscape), so the old beta number (0.55330, certified to 2.1e-4) stands as a valid law-test on a steep single-well quartic — but the dip narrative was false and old gamma never probed the triple well. New protocol rule: design-verification asserts (hB0'(roots)=0, depth checks) must run upstream of both prediction and measurement, since pred-vs-measure agreement cannot catch shared-upstream bugs.
**Real beta (design verified).** Dip landscape f'(roots) = (0.22, 0.40, 0.62), barrier 0.40, dip 0.166 above E0, omega = 0.08. Pre-registered S = 0.45514; measured bracket [0.45486, 0.45580] contains it, |mean−S| = 1.8e-4, b = 0.279. The dip is a spectator: the quadrature integrates through nontrivial topography. CERTIFIED.
**Real gamma (triple-degenerate, H3 violated — the genuine out-of-domain probe).** f(0.25) = f(0.65) exactly. The blind construction lifts the classical degeneracy at O(omega^2): repulsion ~ omega^2/Delta_h is stronger at the near well, the dressed minima split by 0.0155 (hand-estimate omega^2(1/0.25 − 1/0.65) = 0.0157), and the constructor selects x = 0.248 with S_sel = 0.27997 vs alternative S(0→0.65) = 0.41920. Spectroscopy at N = 40 confirms the full predicted architecture: E1−E0 shows exactly one anticrossing (s*/M = 4.101, both states <x>_B = 0.266: A↔x_m); E2−E1 shows the displaced A↔x* anticrossing (s*/M = 4.077, <x>_B = 0.627) on the predicted side with the predicted well character. Measured window exponent Gamma = 0.27680 vs S_sel = 0.27997 — decisively the selected action, not the alternative. **F1 narrowed:** classical degeneracy of h_B minima does NOT cause instanton competition at the gap-min; the coupling itself detunes extensively. Residual F1 territory: *dressed*-degenerate minima (Delta_h-matched wells), codimension-1 — next kill target.

## F1 resolution — dressed-degenerate kill test (final round)
Engineered codimension-1 case: linear tilt delta = -0.039134 tunes the two dressed sheet minima to exact equality (-0.0015380 both), constructor F1 flag TRUE; candidates S1(0->0.249) = 0.28068, S2(0->0.650) = 0.41393. Pre-registered: (P1) anticrossings merge |Ds*/M| < 1e-3; (P2) Gamma = min(S1,S2) = S1.
**P2 PASSES decisively:** measured Gamma (f64, N=20-52) = 0.27369 -- on S1 with the usual window bias, unambiguously not S2 or any mixture. The min-rule holds at exact dressed degeneracy.
**P1 FAILED as registered** (|Ds*/M| = 0.0099 at N=40), then resolved by measurement: extensive Ds* = 0.909/0.416/0.408 at N = 24/40/56 -- an M-independent O(1) offset (~0.41) = subprincipal zero-point detuning between wells. Merger is asymptotic: Ds*/M = c/M. Honest accounting: the asymptotic statement is a corrected claim certified by the three-N scaling, not the pre-registered one.
**F1 verdict: RESOLVED.** The gap-min instanton selection is protected at two layers: (i) generic landscapes -- omega^2-repulsion detunes dressed minima extensively; (ii) classically tuned degeneracy -- O(1) ZPE detuning still isolates a 2-level A<->x_m anticrossing. Even at full (codimension-2) tuning, Perron-Frobenius positivity of both couplings forbids destructive interference in the ground sector, so Gamma = min over candidate actions in every regime; only the spectral architecture changes. Failure mode F1 is removed from the theorem candidate; the constructor's candidate-action table plus min-rule is the certified prescription.

## Codimension-2 coda — min-rule at genuine quantum degeneracy
Per-N tilt tuning (tilt = -0.0391 - 1.088/M) realizes the true 3-level point. Findings: (i) the anticrossings cannot merge below a floor |Ds*| = 2t_B (B-internal tunneling); floor decay rate measured 0.115/N vs back-of-envelope S_B = 0.106 -- the floor IS a measurement of the in-B action; (ii) crossing states couple to the symmetric hybrid (B_m+B_*)/sqrt2 (<x>_B = 0.462 vs midpoint 0.45) -- Perron-Frobenius signs ADD, no destructive channel; (iii) ON the resonance manifold Gamma_loc = 0.27975/0.28207/0.28344/0.28496 rising into S1 = [0.28424, 0.28564]. **Min-rule holds at every layer of fine-tuning.** The fixed-tilt anomaly (0.32222, the program's first pre-registered exponent miss) is resolved as a sweep-through-resonance artifact: an N-dependent resonance crossed by a fixed-parameter window contaminates the fit. Logged as a methodology hazard: resonance-manifold measurements require co-moving tuning.
F1 closure, final form: selection by omega^2-repulsion (generic) -> ZPE detuning (classically tuned, 0.41 measured) -> symmetric hybridization with additive couplings (quantum tuned). Gamma = min over candidate actions in all three regimes; certified, not conjectured.
